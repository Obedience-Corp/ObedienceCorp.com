Campaign-scoped filesystem boundaries. Allowlisted command execution. Cryptographic audit trail for every agent action.

Per-campaign token and execution quotas. Capability profiles from read-only to developer-elevated.

**Trust, but verify. Every action recorded.**
